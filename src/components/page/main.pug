+template('page')
  .page
    .page-grid.-small._dashed
      .page-item(v-for='id in queue')
        check(v-bind:id='id') {{ id }}

    .page-grid._dashed
      .page-item(v-for='id in items')
        check(v-bind:id='id') {{ id }}
